============
Installation
============

At the command line::

    $ easy_install imagematcher

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv imagematcher
    $ pip install imagematcher
